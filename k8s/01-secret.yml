apiVersion: v1
kind: Secret
metadata:
  name: chess-project-secrets
  namespace: chess-project
type: Opaque
stringData:
  # Database
  postgres-user: chess
  postgres-password: chess
  postgres-db: chess
  database-url: "postgresql://chess:chess@postgres:5432/chess"

  # JWT — replace these with real secrets in production!
  jwt-secret: "change-me-generate-a-random-secret"
  jwt-refresh-secret: "change-me-generate-another-random-secret"

  # Server
  port: "3000"
  node-env: production

  # CORS — comma-separated browser origins. Capacitor native origins are allowed by the backend.
  cors-origin: "https://chess-chess-project.apps.ocp-think.levelg.io,http://localhost:5173,http://localhost:3001"
